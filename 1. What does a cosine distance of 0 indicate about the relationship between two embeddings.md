### 1. What does a cosine distance of 0 indicate about the relationship between two embeddings?

- They are similar in direction.
- They are completely dissimilar.
- They have the same magnitude.
- They are unrelated.

------

### **2. A data scientist is exploring Retrieval-Augmented Generation (RAG) for a natural language processing project. Which statement is true about RAG?**

- It is non-parametric and can theoretically answer questions about any corpus.
- It is primarily parametric and requires a different model for each corpus.
- It is not suitable for fact-checking because of high hallucination occurrences.
- It is solely used in QA-based scenarios.

------

### **3. When is fine-tuning an appropriate method for customizing an LLM?**

- When you want to optimize the model without any instructions
- When the LLM already understands the topics necessary for text generation
- When the LLM requires access to the latest data for generating outputs
- When the LLM does not perform well on a particular task and the data required to adapt the LLM is too large for prompt engineering

------

### **4. Which component of Retrieval-Augmented Generation (RAG) evaluates and prioritizes the information retrieved by the retrieval system?**

- Encoder-decoder
- Generator
- Ranker
- Retriever

------

### **5. What is the role of the OnDemandServingMode in the following code snippet?**

```
chat_detail.serving_mode = oci.generative_ai_inference.models.OnDemandServingMode(model_id="ocid1.generativeaimodel.oc1.eu-frankfurt-1.xxxxxxxxxxxxxxxxx")
```

- It configures the model to use batch processing for requests.
- It initializes the model with the default configuration profile for inference.
- It specifies that the Generative AI model should serve requests only on demand, rather than continuously.
- It defines the retry strategy for handling failures during model inference.

------

### **6. A startup is using Oracle Generative AI's on-demand inferencing for a chatbot... How many transactions will be billed for this inference call?**

- 1 transaction per API call, regardless of length
- 500 transactions
- 700 transactions
- 200 transactions

------

### **7. In the context of generating text with a Large Language Model (LLM), what does the process of greedy decoding entail?**

- Using a weighted random selection based on a modulated distribution
- Selecting a random word from the entire vocabulary at each step
- Picking a word based on its position in a sentence structure
- Choosing the word with the highest probability at each step of decoding

------

### **8. You are debugging and testing an OCI Generative AI chat model. What is the model behavior if you don't provide a value for the seed parameter?**

- The model assigns a default seed value of 9999.
- The model generates responses deterministically.
- The model gives diverse responses.
- The model restricts the maximum number of tokens that can be generated.

------

### **9. A machine learning engineer is exploring T-Few fine-tuning... Which of these best describes the characteristic of T-Few fine-tuning for LLMs?**

- It increases the training time as compared to Vanilla fine-tuning.
- It does not update any weights but restructures the model architecture.
- It selectively updates only a fraction of the model's weights.
- It updates all the weights of the model uniformly.

------

### **10. What is the role of the inputs parameter in the given code snippet?**

```
inputs = [
 "Learn about the Employee Stock Purchase Plan",
 "Reassign timecard approvals during leave",
 "View my payslip online",
]
embed_text_detail.inputs = inputs
```

- It controls the maximum number of embeddings the model can generate.
- It provides metadata about the embedding process.
- It sets the output format for the embeddings.
- It specifies the text data that will be converted into embeddings.

------

### **11. What is the purpose of the endpoint variable in the following code?**

```python
endpoint = "https://inference.generativeai.eu-frankfurt-1.oci.oraclecloud.com"
```

- It defines the URL of the OCI Generative AI inference service.
- It specifies the availability domain where the service is hosted.
- It sets the retry strategy for failed requests.
- It stores the API key for authentication.

------

### **12. What is the embedding size of the cohere.embed-english-light-v3.0 model?**

- 384
- 512
- 1024
- 256

------

### **13. Which formula correctly represents the calculation of totalTrainingSteps?**

- totalTrainingSteps = (totalTrainingEpochs + size(trainingDataset)) * trainingBatchSize
- totalTrainingSteps = (size(trainingDataset) * trainingBatchSize) / totalTrainingEpochs
- totalTrainingSteps = (totalTrainingEpochs * trainingBatchSize) / size(trainingDataset)
- totalTrainingSteps = (totalTrainingEpochs * size(trainingDataset)) / trainingBatchSize

------

### **14. What is the difference between Groundedness and Answer Relevance in evaluating LLM responses?**

- Groundedness pertains to factual correctness, while Answer Relevance concerns query relevance.
- Groundedness pertains to query relevance, while Answer Relevance concerns factual correctness.
- Groundedness relates to context understanding, while Answer Relevance focuses on syntax correctness.
- Groundedness ensures data integrity, while Answer Relevance ensures response diversity.

------

### **15. Which of the following does NOT apply when preparing PDF documents for use with OCI Generative AI Agents?**

- PDFs can include images and charts.
- Tables should be structured with rows and columns.
- Hyperlinks are typically not directly surfaced in responses.
- Charts must be two-dimensional with labeled axes.

------

### **16. What kind of relationships do vector databases capture, and why are they essential for LLMs?**

- Hierarchical relationships, useful for organizing data into tree structures.
- Linear relationships, useful for simple comparisons between data points.
- Semantic relationships, crucial for understanding context and generating precise language.
- Temporal relationships, important for tracking time-based changes in data.

------

### **17. What does the 'loss' metric indicate in the context of model training?**

- The number of predictions the model made during training.
- The number of predictions the model made correctly.
- How far the model’s predictions deviate from the actual values.
- The accuracy of the model, which should increase over time.

------

### **18. A company is using a generative AI model that has been recently marked as deprecated. What should the company do next?**

- Continue using the model but start planning to migrate to another model before it is retired.
- Request an extension from the provider to continue using the deprecated model indefinitely.
- Immediately stop using the model and delete all associated resources.
- Ignore the deprecation notice and continue using the model without changes.

------

### **19. How does increasing the temperature parameter affect the output of a language model?**

- Decreasing the temperature broadens the probability distribution, leading to more diverse outputs.
- Increasing the temperature removes the impact of the most likely word in the distribution.
- Increasing the temperature has no effect on the probability distribution of tokens.
- Increasing the temperature flattens the distribution, allowing for more varied word choices.

------

### **20. What happens when you delete a data source used by an OCI Generative AI Agent?**

- The agent automatically ingests another data source to compensate for the deleted one.
- The agent no longer answers questions related to the deleted source.
- The agent uses pretrained data instead of the deleted source.
- The agent stops running completely until a new data source is added.

------

### **21. What is the best approach for building a modular and composable LLM application in LangChain?**

- Using Python classes like LLMChain
- Using prompts only
- Using LangChain Expression Language (LCEL)
- Using agents exclusively

------

### **22. A company’s chatbot generates incorrect product features that are not present in its database. What is the most likely reason?**

- The model is unable to access the database.
- The prompt is ambiguous.
- The model is hallucinating, generating information not grounded in data.
- The model is overfitting to training data.

------

### **23. What happens when generative_ai_inference_client.embed_text(...) is executed?**

- It initializes the embedding model.
- It processes the OCI configuration profile.
- It sends a request to the OCI Generative AI service to generate embeddings for the input text.
- It authenticates the API connection.

------

### **24. How does fine-tuning improve efficiency in LLM usage?**

- It eliminates the need for annotated data.
- It reduces the number of tokens needed for model performance.
- It increases the model’s context window.
- It improves the model’s understanding of human preferences.

------

### **25. When is soft prompting an appropriate technique?**

- When adapting a model to a new domain with large datasets.
- When a large labeled dataset is available for training.
- When there is a need to add learnable parameters without full task-specific training.
- When performing continued pretraining on unlabeled data.

------

### **26. Which parameter ensures identical outputs across multiple runs of the same input?**

- temperature
- top_p
- frequency_penalty
- seed

------

### **27. A company needs to deploy at least 60 endpoints. How many dedicated AI clusters are required if one cluster supports up to 30 endpoints?**

- 5
- 1
- 2
- 3

------

### **28. What happens to an endpoint’s status when it is moved between compartments in OCI?**

- The status changes to Updating during the move and returns to Active after completion.
- The status remains Active throughout the move.
- The endpoint becomes inactive permanently.
- The endpoint is deleted and recreated in the new compartment.

------

### **29. Which of the following data sources is NOT directly supported by OCI Generative AI Agents?**

- Oracle Database 23ai vector search
- Custom-built file systems
- OCI OpenSearch
- OCI Object Storage

------

### **30. What does OCI Generative AI service offer?**

- A limited platform without model hosting capabilities
- Fully managed LLMs along with the ability to create custom fine-tuned models
- Only pretrained models without customization
- Requires sharing GPUs with other customers

------

### **31. Which fine-tuning methods are supported for the cohere.command-r-08-2024 model?**

- T-Few, LoRA, and Vanilla fine-tuning
- T-Few and LoRA
- LoRA and Vanilla fine-tuning
- T-Few and Vanilla fine-tuning

------

### **32. What is the role of indexing in a vector database?**

- Compressing data to reduce storage requirements
- Categorizing data based on predefined labels
- Mapping vectors to a data structure for faster searching
- Converting vectors into a non-indexed format

------

### **33. What must you do before deleting a knowledge base in OCI Generative AI Agents?**

- Archive the knowledge base for backup
- Disconnect any database tool connections
- Delete the data sources and agents using that knowledge base
- Reassign the knowledge base to another agent

------

### **34. What happens to chat data after a session ends in OCI Generative AI?**

- It is permanently deleted immediately
- It is stored in isolation for future customer usage but not used for training
- It is used to improve and train the model globally
- It is archived only for audit purposes

------

### **35. Which phase of a RAG pipeline includes loading documents, splitting them into chunks, and generating embeddings?**

- Retrieval
- Ingestion
- Evaluation
- Generation

------

### **36. What is the typical port range required for Oracle Database connectivity in OCI?**

- 1521–1522
- 8080–8081
- 3306–3307
- 1433–1434

------

### **37. In which phase of the RAG pipeline does the model combine the user query with retrieved context to produce a response?**

- Evaluation
- Ingestion
- Retrieval
- Generation

------

### **38. What is the key difference between fine-tuning and parameter-efficient fine-tuning (PEFT)?**

- Fine-tuning updates all model parameters, while PEFT updates only a subset
- Fine-tuning does not modify the model, while PEFT does
- Fine-tuning trains the model from scratch, while PEFT uses pretrained weights
- Fine-tuning replaces the model architecture, while PEFT keeps it unchanged

------

### **39. Which data type is used to store embeddings in Oracle Database 23ai?**

- INT
- Float32
- Double
- VECTOR

------

### **40. What does multi-modal parsing enable in OCI Generative AI Agents?**

- Automatically tagging files with metadata
- Converting unsupported file formats into text
- Merging multiple data sources into a single dataset
- Parsing and including information from charts and graphs

Here are the **final image extractions (Q41–Q50)**, kept **complete and unchanged with full options**:

------

### **41. What does accuracy measure in model evaluation?**

- The depth of the neural network
- The total number of predictions made
- The proportion of correct predictions out of all predictions
- The proportion of incorrect predictions

------

### **42. At what points does memory interact within a LangChain chain?**

- Only after the output is generated
- Continuously throughout execution
- Before chain execution and after core logic but before output
- Before user input is received

------

### **43. How can the probability distribution of tokens in an LLM be influenced?**

- By restricting the vocabulary size
- By adjusting token size
- By modifying only the training data
- By using techniques like prompting and training

------

### **44. What is the purpose of the VECTOR field in Oracle Database 23ai?**

- To store document IDs
- To store embeddings generated from text
- To store URL references
- To store document titles

------

### **45. What does content moderation configuration in OCI Generative AI Agents control?**

- The complexity of language used in responses
- The maximum size of uploaded files
- The type of vector search used
- Whether moderation applies to inputs, outputs, or both

------

### **46. Which configuration is most likely to increase hallucination in LLM outputs?**

- temperature = 0.0, top_p = 1.0
- temperature = 0.2, top_p = 0.5
- temperature = 0.9, top_p = 0.8
- temperature = 0.0, top_p = 0.2

------

### **47. How many units are required for provisioning a fine-tuning cluster in OCI Generative AI?**

- 8
- 1
- 6
- 2

------

### **48. What is the effect of the presence penalty parameter in text generation?**

- It penalizes tokens that have already appeared in the generated text
- It penalizes tokens only after they appear more than twice
- It penalizes tokens that have not appeared yet
- It penalizes all tokens equally

------

### **49. Why is diffusion modeling challenging for text generation?**

- Diffusion models can only produce images
- Text is not categorical
- Text representation is discrete (categorical) unlike continuous data
- Text generation does not require complex models

------

### **50. What does the "top_p" parameter control in text generation?**

- The number of top tokens considered
- The cumulative probability threshold for token selection
- The penalty applied to frequent tokens
- The maximum number of tokens generated

------

